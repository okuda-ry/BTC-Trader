# Crypto Auto Trader

GMOコイン取引所と Claude AI を組み合わせた、マルチ通貨対応の暗号資産自動売買システム。

BTC / ETH / XRP / SOL の4銘柄を同時に運用し、テクニカル指標を基に Claude AI が売買を判断する。
指値注文（Maker）をデフォルトとし、GMOコインの Maker リベート（-0.01%）を活用する。
Webダッシュボードからリアルタイムで全通貨の状況を監視・制御できる。

---

## 目次

- [特徴](#特徴)
- [システム概要](#システム概要)
- [対応通貨](#対応通貨)
- [動作フロー](#動作フロー)
- [セットアップ](#セットアップ)
- [使い方](#使い方)
- [設定リファレンス](#設定リファレンス)
- [リスク管理の仕組み](#リスク管理の仕組み)
- [損失シミュレーション](#損失シミュレーション)
- [プロジェクト構成](#プロジェクト構成)
- [トラブルシューティング](#トラブルシューティング)
- [注意事項・免責事項](#注意事項免責事項)

---

## 特徴

- **マルチ通貨同時運用** — BTC, ETH, XRP, SOL を同時にトレード（通貨ごとに有効/無効切替可能）
- **AI 売買判断** — Claude AI がテクニカル指標を分析し、BUY / SELL / HOLD を判断
- **指値注文（Maker）** — デフォルトで指値注文を使用し、手数料リベートを獲得
- **自動損切り・利確** — AI判断とは独立して、設定したラインで自動執行
- **追加購入（ナンピン）** — 目標比率未達の場合、加重平均で追加購入
- **Web ダッシュボード** — ブラウザから全通貨のモニタリング・制御が可能
- **エントリー価格永続化** — 再起動しても損益管理が途切れない
- **Dry Run モード** — 実際の注文を出さずに動作確認が可能
- **CLI 分析モード** — Claude Code CLI を使えばAPI利用料が不要（サブスク内で利用可能）

---

## システム概要

```
┌─────────────────────────────────────────────────────────┐
│                    Flask Web App (app.py)                │
│                    http://localhost:5000                 │
├─────────────────────────────────────────────────────────┤
│                                                         │
│    TradeManager (trade_manager.py)                       │
│    ├── CurrencyTrader("BTC") ──┐                        │
│    ├── CurrencyTrader("ETH")   ├── GMOClient (共有)      │
│    ├── CurrencyTrader("XRP")   │   └── REST API          │
│    └── CurrencyTrader("SOL") ──┘       (認証・注文・残高)  │
│         │                                               │
│         ├── RiskManager     損切り/利確/ポジション管理      │
│         ├── candle_builder  ローソク足取得 (GMO/CC)        │
│         ├── indicators      RSI / MACD / BB 計算          │
│         └── ai_analyzer     Claude AI 売買判断            │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 対応通貨

| シンボル | 銘柄 | 最小注文 | 損切り | 利確 | 特徴 |
|---------|------|---------|--------|------|------|
| **BTC** | ビットコイン | 0.00001 | -2% | +4% | 基軸通貨、流動性最大 |
| **ETH** | イーサリアム | 0.001 | -3% | +5% | 時価総額2位、DeFi基盤 |
| **XRP** | リップル | 1 | -3% | +6% | 国内人気高、流動性高 |
| **SOL** | ソラナ | 0.01 | -3% | +6% | 高速チェーン、成長著しい |

各通貨は総資産の **10%** を上限に独立運用される（最大4通貨で40%投入、60%はJPYで待機）。

設定は [`currencies.json`](currencies.json) で通貨ごとにカスタマイズ可能。

---

## 動作フロー

```
15分ごとのサイクル
│
├─ 1. 全通貨のローソク足を一括取得（GMOコイン klines API）
│     └─ 取得失敗時は CryptoCompare API にフォールバック
│
├─ 2. テクニカル指標を計算
│     ├─ RSI (14期間)
│     ├─ MACD (12/26/9)
│     └─ ボリンジャーバンド (20期間, 2σ)
│
├─ 3. 通貨ごとに以下を実行:
│     │
│     ├─ 未約定の指値注文があれば状態確認
│     │   ├─ 約定済み → ポジション/残高を更新
│     │   ├─ 未約定 → キャンセルして次のサイクルで再判断
│     │   └─ 部分約定 → 約定分を反映
│     │
│     ├─ ポジション保有中なら損切り/利確チェック
│     │   ├─ 損切りライン到達 → 成行で即売却
│     │   └─ 利確ライン到達 → 指値で売却
│     │
│     ├─ Claude AI にテクニカル指標 + 他通貨の状況を送信
│     │   └─ BUY / SELL / HOLD + 確信度 + 理由 を受信
│     │
│     └─ 確信度 60% 以上なら売買を実行
│         ├─ BUY: 目標比率未達なら購入（追加購入も可）
│         └─ SELL: 全ポジションを売却
│
└─ 4. 15分間待機 → 次のサイクルへ
```

---

## セットアップ

### 1. リポジトリのクローン

```bash
git clone https://github.com/okuda-ry/BTC-Trader.git
cd BTC-Trader
```

### 2. Python 仮想環境の作成

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Mac / Linux
source venv/bin/activate
```

### 3. パッケージのインストール

```bash
pip install -r requirements.txt
```

### 4. 環境変数の設定

```bash
cp .env.example .env
```

`.env` を編集:

```env
# GMOコイン API（必須）
GMO_API_KEY=あなたのGMOコインAPIキー
GMO_API_SECRET=あなたのGMOコインシークレット

# Claude 分析モード
ANALYZER_MODE=cli
```

#### GMOコイン APIキーの取得

1. [GMOコイン](https://coin.z.com/jp/) にログイン
2. 会員ページ → API → 新しいAPIキーを作成
3. 以下の権限を有効にする:
   - **現物取引** — 注文・キャンセル
   - **資産残高の参照** — 残高確認用
4. APIキーとシークレットを `.env` に設定

#### Claude 分析モード

| モード | 設定値 | コスト | 備考 |
|--------|--------|--------|------|
| **Claude Code CLI** | `cli` | 無料（サブスク内） | Claude Code がインストール済みであること |
| **Anthropic API** | `api` | 従量課金 | `.env` に `ANTHROPIC_API_KEY` も設定 |

### 5. Claude Code のインストール（CLI モード使用時）

```bash
npm install -g @anthropic-ai/claude-code
```

---

## 使い方

### Web アプリケーション（推奨）

```bash
python app.py
```

ブラウザで **http://localhost:5000** にアクセス。

#### ダッシュボードの操作

1. **Dry Run / LIVE トグル** でモードを選択
   - Dry Run: 注文を出さずに分析のみ実行
   - LIVE: 実際に注文を出す（自己責任）
2. **開始ボタン** でトレーディングを開始
3. 通貨タブ（BTC / ETH / XRP / SOL）で各通貨の状態を確認
4. **停止ボタン** でトレーディングを停止

#### ダッシュボードの表示内容

- 現在価格、テクニカル指標（RSI / MACD / BB）
- ポジション情報（保有量、エントリー価格、損益率）
- AI の判断（BUY / SELL / HOLD + 確信度 + 理由）
- 取引履歴（約定済みのみ記録）
- リアルタイムログ

### CLI

```bash
# ドライラン（分析のみ）
python main.py

# 1回だけ分析して終了
python main.py --once

# 本番モード
python main.py --live

# デバッグログ付き
python main.py --log-level DEBUG
```

---

## 設定リファレンス

### グローバル設定 (`config.py`)

| 項目 | デフォルト | 説明 |
|------|-----------|------|
| `TRADE_INTERVAL_SEC` | 900（15分） | 分析・売買サイクルの間隔 |
| `CANDLE_PERIOD_SEC` | 3600（1時間） | ローソク足の時間枠 |
| `CANDLE_COUNT` | 100 | 取得するローソク足の本数 |
| `ORDER_TYPE` | `LIMIT` | 注文方式（`LIMIT` / `MARKET`） |
| `LIMIT_OFFSET_PCT` | 0.05 | 指値のオフセット（現在価格からのずれ%） |
| `MIN_TRADE_JPY` | 1,000 | 最小取引額（円）。これ未満の注文は出さない |

### テクニカル指標パラメータ (`config.py`)

| 項目 | デフォルト | 説明 |
|------|-----------|------|
| `RSI_PERIOD` | 14 | RSI の計算期間 |
| `MACD_FAST` | 12 | MACD 短期EMA |
| `MACD_SLOW` | 26 | MACD 長期EMA |
| `MACD_SIGNAL` | 9 | MACD シグナル線 |
| `BB_PERIOD` | 20 | ボリンジャーバンド期間 |
| `BB_STD` | 2 | ボリンジャーバンド標準偏差倍率 |

### 通貨別設定 (`currencies.json`)

| 項目 | 説明 |
|------|------|
| `enabled` | `true` で自動売買を有効化 |
| `name` | 日本語表示名 |
| `min_order_size` | GMOコインの最小注文単位 |
| `size_decimals` | 注文数量の小数桁数 |
| `max_position_ratio` | 総資産に対するこの通貨の目標投入比率 |
| `stop_loss_pct` | 損切りライン（エントリー価格からの下落率） |
| `take_profit_pct` | 利確ライン（エントリー価格からの上昇率） |

---

## リスク管理の仕組み

### ポジションサイズ制御

- 各通貨は総資産の **10%** を上限に運用（`max_position_ratio`）
- 4通貨合計で最大 **40%** をポジションに投入、**60%** はJPYで待機
- 最小取引額（1,000円）未満の注文はスキップ

### 追加購入（買い増し）

目標比率（10%）に未達の場合、AIがBUY判断を出せば差分を追加購入する。

```
例: 総資産 10万円、BTC目標 = 1万円分
  現保有: 3,000円分 → 差分 7,000円分を追加購入
  平均取得単価は加重平均で自動計算
```

### 損切り・利確

| 種類 | トリガー | 執行方法 | 優先度 |
|------|---------|---------|--------|
| **損切り** | エントリー価格から一定%下落 | **成行注文**（即約定） | AI判断より優先 |
| **利確** | エントリー価格から一定%上昇 | 指値注文 | AI判断より優先 |

損切りは成行で即執行されるため、スリッページが発生する可能性はあるが確実に約定する。

### 指値注文のライフサイクル

```
指値注文送信 → 次のサイクル（15分後）で状態確認
  ├─ 約定済み → ポジション更新、取引履歴に記録
  ├─ 未約定 → キャンセル → 次のサイクルで再判断
  ├─ 部分約定 → 約定分を反映、残りはキャンセル
  └─ 確認エラー → 3回リトライ → キャンセル試行後クリア
```

未約定の注文がある間は新規注文を出さないため、連続購入は発生しない。

### エントリー価格の永続化

`risk_state.json` に通貨ごとのエントリー価格と保有量を保存。
アプリを再起動しても損益管理が途切れない。

---

## 損失シミュレーション

10万円を運用した場合の最悪シナリオ分析。

### 1回の全通貨同時損切り

| 通貨 | 投入額 | 損切り% | 損失額 |
|------|--------|---------|--------|
| BTC | ¥10,000 | -2% | -¥200 |
| ETH | ¥10,000 | -3% | -¥300 |
| XRP | ¥10,000 | -3% | -¥300 |
| SOL | ¥10,000 | -3% | -¥300 |
| **合計** | **¥40,000** | | **-¥1,100** |

1回の最大損失: **約 -1,100円（-1.1%）**

### 資産が半減（5万円）になるまで

```
毎回全通貨が同時損切りされる最悪ケースで:
  1回あたり約 -1.1% → 0.989^n = 0.5
  n ≈ 63回（全4通貨同時の損切りが63回連続）
  15分サイクルなら約 15.75 時間
```

現実には利確も混ざるため、この最悪ケースが実現する確率は極めて低い。

### 注意すべきリスク

| リスク | 説明 |
|--------|------|
| **スリッページ** | 急落時に損切りの成行注文が設定値より不利な価格で約定する |
| **API障害** | 取引所のAPI障害で注文が通らない/状態確認ができない |
| **スリープ** | PCがスリープすると損切りが発動しない |
| **相関リスク** | BTC急落時にアルト全体が連動下落し、4通貨同時に損切りされる |

---

## プロジェクト構成

```
BTC-Trader/
├── app.py                  # Flask Webアプリケーション（ダッシュボード）
├── main.py                 # CLI エントリーポイント
├── config.py               # グローバル設定・環境変数
├── currencies.json         # 通貨別設定（4通貨）
│
├── trade_manager.py        # 全通貨オーケストレーター
├── trader.py               # 通貨別トレードロジック (CurrencyTrader)
├── gmo_client.py           # GMOコイン REST API クライアント
├── ai_analyzer.py          # Claude AI 売買判断（CLI / API 切替）
├── candle_builder.py       # ローソク足取得（GMO / CryptoCompare）
├── indicators.py           # テクニカル指標計算（RSI / MACD / BB）
├── risk_manager.py         # リスク管理（損切り/利確/サイズ/永続化）
│
├── risk_state.json         # エントリー価格の永続化ファイル（自動生成）
├── trader.log              # ログファイル（自動生成）
│
├── templates/
│   └── index.html          # ダッシュボード画面（通貨タブ対応）
├── static/
│   └── style.css           # ダークテーマ スタイルシート
│
├── docs/
│   ├── multi_currency_design.md   # マルチ通貨 外部設計書
│   ├── architecture.md            # アーキテクチャ詳細
│   ├── gmo_api_reference.md       # GMOコイン API リファレンス
│   └── trading_strategy.md        # トレーディング戦略の解説
│
├── .env.example            # 環境変数テンプレート
├── .gitignore
├── requirements.txt        # Python 依存パッケージ
└── README.md               # このファイル
```

### 各ファイルの役割

| ファイル | 役割 |
|---------|------|
| `app.py` | Flask サーバー。ダッシュボード表示、API エンドポイント、バックグラウンドでトレードスレッド管理 |
| `trade_manager.py` | 全通貨のサマリ一括取得 → 各 CurrencyTrader に配布してサイクル実行 |
| `trader.py` | 1通貨の完全な売買サイクル（指値管理、損切り、AI判断、注文送信） |
| `gmo_client.py` | HMAC-SHA256 認証、Public/Private API ラッパー |
| `ai_analyzer.py` | Claude にテクニカル指標を渡し、JSON形式で判断を受け取る。応答のバリデーション付き |
| `candle_builder.py` | GMOコイン klines API → CryptoCompare の順にフォールバック |
| `indicators.py` | pandas による RSI / MACD / ボリンジャーバンド計算 |
| `risk_manager.py` | エントリー価格の加重平均、損切り/利確判定、`risk_state.json` への永続化 |

---

## トラブルシューティング

### ローソク足が不足するエラー

```
[BTC] ローソク足データが不足 (28本)
```

GMOコインのklines APIは当日分のデータが少ない場合があります。複数日分を自動取得するため、初回起動時は数時間待てば十分なデータが蓄積されます。

### Claude CLI の認証エラー

```
Claude CLI 応答なし (code=1): Invalid API key
```

`.env` に `ANTHROPIC_API_KEY` が設定されていると、CLI モードと競合します。CLI モードでは自動的に `ANTHROPIC_API_KEY` を環境から除外しますが、解消しない場合は `.env` から当該行を削除してください。

### 指値注文が約定しない

指値注文は現在価格から `LIMIT_OFFSET_PCT`（デフォルト0.05%）だけ有利な価格で出します。価格変動が小さい場合は約定しないことがありますが、次のサイクル（15分後）で自動キャンセル → 再判断されます。

### PCスリープ時の挙動

PCがスリープすると全処理が停止し、損切りが発動しません。LIVEモードで運用する場合は **スリープを無効** にしてください。

Windows: 設定 → システム → 電源とスリープ → 「なし」に設定

### ポジションがあるのにBUYが出る

目標比率（10%）に未達の場合は追加購入が実行されます。これは正常な動作です。ログに「目標比率未達のため追加購入」と表示されます。

---

## 注意事項・免責事項

- **投資は自己責任です。** 本ボットの利用による損失について、開発者は一切の責任を負いません
- まずは **Dry Run モード** で十分に動作を確認してください
- LIVE モード運用中は **PCのスリープを無効** にしてください
- GMOコインのAPI利用規約を遵守してください
- 本ボットは教育・研究目的で開発されています
